# AI Usage Log — Developer 1

- AI assistance was used to implement the Product hierarchy and its persistence/service integration.
- Assistance covered Java structure, inheritance, polymorphism, validation, file serialization, and the ProductGateway contract used by sales.
- The resulting module contains `Product`, `VideoGame`, `Console`, `ProductRepository`, and `ProductService`.
